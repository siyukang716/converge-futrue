<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cloud.sys.mapper.UserMapper">

    <select id="listUsers" resultType="com.cloud.sys.UserEntity">
        SELECT 	user.*,
        sud.dept_id  ,
        r.role_name roleName FROM
            `user`  LEFT JOIN user_role ur ON user.id = ur.user_id
                    LEFT JOIN role r on r.id = ur.role_id
                    LEFT JOIN sys_user_dept sud ON sud.user_id = user.id
        ${ew.customSqlSegment}
    </select>

    <!--恢复用户-->
    <update id="recoverUser">
        UPDATE `USER` SET is_del = 1 WHERE id = #{id}
    </update>

</mapper>